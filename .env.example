# ---------- Datenbank ----------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=eateasy
DB_USER=eateasy
DB_PASSWORD=eateasy

# ---------- Mailer (Maildev lokal) ----------
MAIL_HOST=localhost
MAIL_PORT=1025

# ---------- Ollama ----------
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3

# ---------- Externe APIs ----------
SPOONACULAR_API_KEY=
# OpenFoodFacts (Barcode-Lookup) — Default reicht, nur bei Self-Hosting setzen.
# OPENFOODFACTS_URL=https://world.openfoodfacts.org
