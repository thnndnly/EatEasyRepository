# ---------- HTTP ----------
quarkus.http.port=8080
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=http://localhost:5173
quarkus.http.cors.methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
quarkus.http.cors.headers=Authorization,Content-Type
quarkus.http.cors.exposed-headers=Content-Disposition

# ---------- Datasource ----------
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USER:eateasy}
quarkus.datasource.password=${DB_PASSWORD:eateasy}
quarkus.datasource.jdbc.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:eateasy}

# ---------- Hibernate ORM (Panache) ----------
quarkus.hibernate-orm.database.generation=validate
quarkus.hibernate-orm.log.sql=false

# ---------- Flyway ----------
quarkus.flyway.migrate-at-start=true
quarkus.flyway.baseline-on-migrate=true
quarkus.flyway.locations=classpath:db/migration

# ---------- SmallRye JWT ----------
mp.jwt.verify.publickey.location=publicKey.pem
mp.jwt.verify.issuer=https://eateasy.local
smallrye.jwt.sign.key.location=privateKey.pem
quarkus.smallrye-jwt.enabled=true

# ---------- Mailer ----------
quarkus.mailer.from=noreply@eateasy.local
quarkus.mailer.host=${MAIL_HOST:localhost}
quarkus.mailer.port=${MAIL_PORT:1025}
quarkus.mailer.start-tls=DISABLED
quarkus.mailer.mock=false

# ---------- Ollama (Smart-Suggestion) ----------
ollama.url=${OLLAMA_URL:http://localhost:11434}
ollama.model=${OLLAMA_MODEL:llama3}

# ---------- Externe Integrationen ----------
integration.spoonacular.api-key=${SPOONACULAR_API_KEY:}
integration.openfoodfacts.url=https://world.openfoodfacts.org

# ---------- Dev-Profil ----------
%dev.quarkus.hibernate-orm.log.sql=true
%dev.quarkus.log.category."de.eateasy".level=DEBUG

# ---------- Test-Profil ----------
# Dev Services startet automatisch einen Postgres-Testcontainer (Docker erforderlich).
# JDBC-URL und Credentials werden zur Laufzeit eingespritzt.
%test.quarkus.datasource.devservices.enabled=true
%test.quarkus.datasource.devservices.image-name=postgres:16
%test.quarkus.datasource.username=
%test.quarkus.datasource.password=
%test.quarkus.datasource.jdbc.url=
%test.quarkus.flyway.migrate-at-start=true
%test.quarkus.mailer.mock=true
